import 'package:flutter/material.dart';

class CustomColors {
  //Colors of layout
  static const Color background = Color.fromARGB(255, 0, 96, 134);
  static const Color transparent = Colors.transparent;

  static const Color primary = Color.fromARGB(255, 0, 183, 255);
  static const Color textButton = Colors.black;
}
